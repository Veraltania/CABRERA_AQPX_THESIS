Prompt Log: Optimization and experimental tool configuration for EA-based PI tuning using Google Gemini Pro

Create a version of this script that instead has the idea to analyze whether a PI controller tuned on earlier dates (Feb 5) can still perform well after 20 days later (Feb 25). i was thinking of primarily doing it based on daytime data, with the PI controller tuned on Feb 5 daytime and then tested on Feb 25 and Feb 26 (whole day, as in both day and night). retain the same type of analyses (IAE, AUC) and style of graphing as before. STRIP AWAY THE MATLAB AND LAMBDA GAINS. 

Create a version of step_response_comparison that instead compares the algorithms in terms of load disturbance rejection. strip away the setpoint tracking, just focus on load disturbance rejection. i want the analysis / graphing of the load disturbance rejection to be similar to the MATLAB script attached.

I want to retain the comparison and graphing logic of this script, but it should be extended so that it now plots the load disturbance rejection of each controller gain being compared. per transfer function, universalize the x and y-axes so that the results are easily comparable.

Expand this script to use the root locus analysis (from the python controls library) and graphing (output as a separate graph in the same directory as the other graphs) of each tuning strategy. it should be able to compute the gain at the imaginary crossing, as well as the gain at the 50% damping ratio on the root locus. use python's control library and numerical analysis methods.

Create a version of this script that instead conducts a sweep of the cf_weight variable (without argparse or command-line, i should be able to write within the code's main function itself the start point, endpoint, and number of bins), with the corresponding decrease in perf_weight. then, the final output is a graph comparing all the PI controllers with different cf_weights, alongside their statistics in terms of IAE and control effort. the idea is to visualize the trade-off between IAE/error tracking and control effort. retain the iteration through each row in the CSV, since that is where i specified the transfer functions. each transfer function = one final graph + corresponding statistics.